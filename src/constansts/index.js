//Importing all the constants at one place and then exporting it
import lowerButtons from "./lowerButtons";
import upperButtons from "./upperButtons";
import paperTypes from "./paperTypes";

export { lowerButtons, upperButtons, paperTypes};
